import './styles.css'

export function createContactSection() {
    return `
<div class="contact island bg-black">
        <h2 contenteditable data-key="contact.title">Let´s chat! I´m ready to work on excinting projects</h2>
        <span contenteditable data-key="contact.mail" class="sbt-2 contact__email">srkarthik.designscape@gmail.com</span></div>
`
}
